server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/bioskop_db
    username: postgres
    password:
    driver-class-name: org.postgresql.Driver


